# CYBERCEL API - Configuração compatível com Render
DirectoryIndex index.php

# Ativar rewrite engine
RewriteEngine On

# Se o arquivo ou diretório não existir, redireciona para api.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ api.php [QSA,L]

# CORS headers via PHP (não via .htaccess)
# Removido Header set porque causa erro no Render
